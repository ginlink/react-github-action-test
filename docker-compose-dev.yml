version: '3'

services:
  web:
    image: coinflow/heco-converters-web:dev
    container_name: web
    restart: always
    ports:
      - '80:80'
    privileged: true

networks:
  lend_default:
    driver: bridge
